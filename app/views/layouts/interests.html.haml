!!! 5
%html{ lang: "en"}
  = render "layouts/head", title: interest_head_title
  %body{class: "#{app_theme} application", :'data-page' => body_data_page}
    = render "layouts/broadcast"
    = render "layouts/head_panel", title: "interest: #{@interest.name}"
    = render "layouts/flash"
    %nav.main-nav.navbar-collapse.collapse
      .container= render 'layouts/nav/interest'

    .container
      .content= yield
